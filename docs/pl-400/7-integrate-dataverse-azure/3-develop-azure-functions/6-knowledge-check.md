# Module assessment

## Check your knowledge

1. Which of the following choices is required for a function to run?

    - Binding
    - **Trigger**
    - Both triggers and bindings

2. Which of the following choices supports both the `in` and `out` direction settings?

    - **Bindings**
    - Trigger
    - Connection value

3. In the Node.js v4 programming model, how are triggers and bindings configured?

    - **In code using the `@azure/functions` package**
    - By editing function.json directly in the Azure portal
    - By adding entries to host.json
